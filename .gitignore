node_modules/
dist/
*.tsbuildinfo
*.log
.DS_Store
config.json
oauth_client.json
token.json
.claude/settings.local.json
.claude/telemetry/
/payload.json
# Internal reports built from live Jira/Drive data. Local only, never on GitHub.
/reports/
# Codex-convention mirrors of `.claude/`, written by a generator outside this
# repository. `.claude/` is authoritative; a second copy only drifts from it.
/.agents/
/.codex/
