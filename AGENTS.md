# AGENTS.md

The rules for this repository live in `CLAUDE.md`. Read that file.

The `.agents/` and `.codex/` directories are unmanaged generator output and may
be stale — the authoritative agents, skills and hooks are in `.claude/`.
